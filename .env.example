# MongoDB Connection
# Required: MongoDB connection string
# Example: mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority
MONGODB_URI=your-mongodb-connection-string-here

# JWT Secret
# Required: Secret key for signing JWT tokens
# Generate a strong random string (e.g., use: openssl rand -base64 32)
JWT_SECRET=jaNSCJnvcdjVNSJVNjvnivndsoivnkjdVN

# Server Port
# Optional: Port for the server to listen on (defaults to 5000)
# Render automatically sets this, so you don't need to set it manually
PORT=5000

# Redis Configuration (Optional)
# Only needed if you want async CSV processing with queue
# If not set, CSV imports will be processed synchronously
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
